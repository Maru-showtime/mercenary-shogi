# 傭兵将棋（仮題） 仕様書 v0.1

## 0. このドキュメントについて

GitHub Pages で公開する、ブラウザ動作の将棋変種ゲームの仕様書。
実装は Claude Code に本ファイルを読ませて進める。

**コスト値は全て暫定。** 実際に対戦してから調整する前提で実装すること。
コストは `src/data/mercenaries.json` の1か所を直せば変わる構造にする。

---

## 1. ゲームの骨子

通常の将棋に「対局前の編成フェーズ」を追加した変種。

- 盤は **9列 × 9段**（通常の将棋と同じ）
- 初期セットは通常の将棋の駒。コスト0
- **コスト上限10** の範囲で、初期セットの駒を特殊能力を持つ「傭兵」と差し替えられる
- 傭兵は取られると能力が永久に消滅し、ただの通常駒になる
- 傭兵は成っても能力を失う。代わりに一部は「成り特典」を持つ

### 設計の中心にある緊張

1. 高コストの傭兵ほど、取られた時の損失が大きい
2. 成れば駒は強くなるが、傭兵能力は失われる
3. 上限10という枠が、一点賭けかバランス型かの選択を強制する

---

## 2. 技術方針

| 項目 | 決定 |
|---|---|
| 言語 | HTML + CSS + JavaScript (ES Modules) |
| 盤面描画 | **DOM + CSS Grid**（Canvas ではない） |
| AI | ブラウザ内の JavaScript。Web Worker で実行 |
| ビルド | なし |
| 外部ライブラリ | 使用しない |
| ホスティング | GitHub Pages |

### 補足

- 将棋盤は81マスの静的なグリッドなので、DOM の方が実装・スタイリング・アクセシビリティで有利。Canvas は不要
- パスは全て相対パス（`./src/...`）。GitHub Pages はサブディレクトリ配信のため
- ES Modules は `file://` で動かないので、ローカル確認は `python -m http.server` を使う
- 駒は**五角形＋キャラクターのイラスト**で表現する（詳細は「14. ビジュアル仕様」）
- **開発初期は漢字表示で進めてよい。** 絵が揃う前にルール実装を完成させるため、`renderMode: "text" | "sprite"` で切り替えられるようにしておく

---

## 3. 盤とルール

### 盤

- **9列 × 9段**（通常の将棋と同じ）
- 自陣3段 × 2 ＝ 6段、中立地帯は **3段**
- 敵陣＝相手側の3段（通常の将棋と同じ定義）

### 初期配置

通常の将棋とまったく同じ配置。

```
段1（後手）  香 桂 銀 金 玉 金 銀 桂 香
段2（後手）  ・ 飛 ・ ・ ・ ・ ・ 角 ・
段3（後手）  歩 歩 歩 歩 歩 歩 歩 歩 歩
段4          ・ ・ ・ ・ ・ ・ ・ ・ ・
段5          ・ ・ ・ ・ ・ ・ ・ ・ ・
段6          ・ ・ ・ ・ ・ ・ ・ ・ ・
段7（先手）  歩 歩 歩 歩 歩 歩 歩 歩 歩
段8（先手）  ・ 角 ・ ・ ・ ・ ・ 飛 ・
段9（先手）  香 桂 銀 金 玉 金 銀 桂 香
```

### 盤を9×8から9×9に戻した経緯

当初は「通常より1段狭い 9列×8段」で設計していたが、9×9（通常の将棋と同じ）に変更した。

変更前の8段盤には、この仕様書自身が挙げていた副作用があった。

- 中立が2段しかなく、序盤の駆け引きが1手分短縮されていきなり接触する
- 歩が成り込むまでが通常より早い
- 角の利きが初手から効きやすい
- 入玉が通常より容易

これらはいずれも「守りを固める時間がない」方向に働く。囲いを作る余裕がないため、
**攻め合いというより、準備なしの殴り合いになりやすかった。**
9段に戻すと中立が3段になり、上の副作用がすべて緩和される。

副次的な利点として、**通常の将棋の定跡・囲いの感覚がそのまま使える。**
本作の面白さは傭兵の編成にあるので、盤そのものは素直な将棋にしておくほうが、
傭兵の効果が際立つという判断。

盤サイズは `src/core/board.js` の `HEIGHT` 1か所で決まる。
星の位置・段の目盛り・AIの前進評価はすべてそこから導出される。

### 通常ルールの扱い

以下は通常の将棋と同じ。

- 持ち駒の打ち込み
- 二歩の禁止
- 打ち歩詰めの禁止
- 行き所のない駒の禁止（最奥段の歩・香、最奥2段の桂）
- 千日手（同一局面4回で引き分け、連続王手の千日手は王手側の負け）
- 詰みで勝利

### 成り

- 敵陣3段への「入る・出る・敵陣内で動く」いずれかで成れる
- **成りは任意**。ただし行き所のない駒になる場合は強制成り
- **成った時点で傭兵能力は消滅する**（後述）

---

## 4. 編成フェーズ

### ルール

- 使えるコストは **上限10**。余らせてもよい
- 傭兵は **同じ種類の通常駒とのみ差し替え**できる（歩の傭兵は歩と交換）
- 玉も差し替え可能。ただし**玉の傭兵とのみ**
- 同名の傭兵を **複数枚選んでもよい。ただし枚数ぶんのコストを払う**（盾歩2枚なら 5×2＝10）
- 同じ駒種の傭兵の合計枚数は、初期配置にあるその駒の枚数が上限（歩9・香2・桂2・銀2・金2・玉1・角1・飛1）
- 差し替えた駒は初期配置の同じマスに置かれる（配置の自由移動はしない）。同種の駒が複数ある場合、**どのマスに置くかは編成時に選べる**
- 双方の編成は **対局開始前に互いに公開される**（非公開情報なし）

#### 複数枚取りについての補足

コスト上限10が自然なブレーキになっている。影武者(7)×2＝14、龍騎銀(9)×2＝18 はいずれも上限を超えるため選べない。
実際に2枚取りできるのはコスト5以下の傭兵に限られる（盾歩5×2＝10 がちょうど上限）。

例外として、傭兵側に `maxCount` を持たせて個別に上限を設けられる。
**初手駆けの歩は `maxCount: 1`**。一局の最初の1手にしか使えない能力なので、2枚目以降は必ず無駄になるため。

### 玉の傭兵についての設計原則

玉は取られたら負けなので、「取られたら能力消滅」というリスクが玉には存在しない。
そのため**玉の傭兵は必ず能力自体にデメリットを内蔵させること**。
純粋に強くなるだけの玉の傭兵は作らない。

---

## 5. 能力の消滅ルール（本作の中核）

| 契機 | 結果 |
|---|---|
| 取られた | 能力は**永久に消滅**。通常駒として相手の持ち駒になる |
| 取り返した | 能力は**戻らない**。通常駒のまま |
| 成った | 能力は**消滅**。通常の成り駒になる |
| 成り駒が取られた | 通常の将棋通り、元の通常駒として持ち駒になる |

### 成り特典

「成ると能力を失う」ことの見返りとして、一部の傭兵には一度きりの特典を設定する。
特典は成った瞬間に1回だけ発動し、その後は通常の成り駒として振る舞う。

---

## 6. 傭兵リスト

### 歩

| 名称 | コスト | 能力 | 成り特典 |
|---|---|---|---|
| 先駆歩 | 5 | 敵陣に入らなくても、いつでも成れる | なし |
| 盾歩 | 5 | 取られる時、1回だけ耐えてその場に残る | 持ち駒に盾歩が1枚補充される |
| 斜歩 | 4 | 斜め前にも1マス進める | なし |
| 初手駆けの歩 | 1 | 一局の最初の1手に限り、前に2マス進める | なし |

**盾歩の詳細**：「耐える」は、取ろうとした相手の駒がその手を空振りする扱い。
相手の駒は動かず、盾歩もその場に残る。相手の手番は消費される。
発動は一局に1回のみ。発動後は通常の歩になる。

**盾歩と王手の関係（重要）**：
- 王手を受けている側は、**盾歩を取る手で王手を外すことはできない。** 弾かれて何も動かないため、
  王手はかかったままになる。よってこの手は**非合法手**として扱う
- 結果として、**盾歩が王手をかけていて他に逃れる手がなければ詰み**になる
- 合法手の判定（`rules.js` の `simulateBoardMove`）は、この「弾かれると盤が動かない」ことを
  必ず考慮すること。取れた前提で判定すると、王手を外せたつもりの手が合法になり、
  次の手で玉が取られる（＝玉が盤から消えたまま対局が続く）

**初手駆けの歩の詳細**：
- 2マスは**滑る**（途中のマスに駒があれば進めない）。跳び越えは不可
- **一局の最初の1手のみ**。使わずに2手目以降になった時点で能力は消滅する
- 持ち越しは一切しない

### 香

| 名称 | コスト | 能力 | 備考 |
|---|---|---|---|
| 遊撃香 | 4 | 前に走れるうえ、後ろと斜め4方向へ1マス動ける | 真横には動けない |

### 桂

| 名称 | コスト | 能力 | 備考 |
|---|---|---|---|
| 三方桂 | 4 | 桂馬の跳び先2箇所に加えて、正面2マス先へも跳べる（3方向とも2段先へ跳び越す） | 行き所がない場合は強制成り |
| 帰還桂 | 2 | 一局に1回、跳ぶ直前の位置に戻れる | 戻るのに1手を消費する |

### 銀

| 名称 | コスト | 能力 | 備考 |
|---|---|---|---|
| 龍騎銀 | 9 | 成ると**龍**になる | 成った時点で傭兵能力は消滅（以後は龍として振る舞う） |
| 退銀 | 5 | 真後ろにも引ける | |

**設計意図**：銀は前に出やすいが敵陣3段へ入るのは危険、という将棋本来の緊張を強化する。
龍騎銀を取ると残りコストは1しかなく、「初手駆けの歩」しか買えない。一点賭けの構成になる。

### 金

| 名称 | コスト | 能力 |
|---|---|---|
| 影武者 | 7 | 玉が取られる局面で、1回だけ身代わりになって盤上から消える |

**影武者の詳細**：玉に王手がかかり、他に回避手段がない場合に発動。
影武者が盤上から取り除かれ、王手が解除された扱いになる。
影武者は相手の持ち駒にならず消滅する。発動は一局に1回のみ。

### 角・飛

| 名称 | コスト | 能力 |
|---|---|---|
| 竜牙角 | 8 | 最初から馬の動き（角の動き＋縦横1マス） |
| 車輪飛 | 8 | 最初から龍の動き（飛の動き＋斜め1マス） |

いずれも**成りの概念を持たない**（既に成った動きのため）。
取られた場合は通常の角・飛として相手の持ち駒になる。

### 玉

| 名称 | コスト | 能力 |
|---|---|---|
| 蛮勇の王 | 3 | 前に2マス進める。ただし真後ろには下がれない（斜め後ろは可） |

**蛮勇の王の詳細**：
- 前2マスは**滑る**（途中に駒があれば止まる）。跳び越えは不可
- 真後ろ1マスへの移動が禁止される。斜め後ろは通常通り可能
- 斜め後ろ2手で実質的に真後ろへ戻れるため、損失はテンポのみ
- 前2マスは入玉を早める。**バランス調整の要注意点**

### コスト構成の例（上限10）

- 龍騎銀9 ＋ 初手駆けの歩1
- 竜牙角8 ＋ 帰還桂2
- 盾歩5 ＋ 斜歩4 ＋ 初手駆けの歩1
- 影武者7 ＋ 蛮勇の王3
- 遊撃香4 ×2 ＋ 初手駆けの歩1
- 先駆歩5 ＋ 退銀5
- 盾歩5 ＋ 退銀5 ＋ 帰還桂2 → 合計12のため不可

---

## 7. 未採用・検討中の案

実装しないが、バランス調整で必要になった場合の候補として記録する。

### 一度実装したが廃止したもの

種類が多すぎて選ぶ楽しさより煩雑さが勝っていたため、2026年9月に3種を廃止した。
効果タイプ自体はエンジンに残っているので、復活させたければ JSON に戻すだけでよい。

- **隠形歩（3）**取られても相手の持ち駒にならず盤外へ消える → 効果は地味で、
  盤上の見た目にも表れないため廃止。`vanishOnCapture` の実装は残っている
- **直走香（6）**前後どちらにも直線で走れる（成れない）→ 遊撃香と役割が重なるため廃止
- **馬廻銀（8）**成ると馬になる → 龍騎銀(9)とほぼ同じ選択肢で、片方あれば足りるため廃止

同時に、**反転香（2・後ろへ1マス）を「遊撃香（4）」に作り直した。**
後ろ1マスだけでは弱すぎて誰も選ばなかったため、斜め4方向を足して機動力の駒にした。

- **越香（5）**味方の駒を1つ飛び越える → 狭い盤では強すぎるため不採用
- **号令金（6）**隣接する味方の歩が2マス進める → 他駒への干渉はルールも実装も複雑化するため不採用
- **マイナスコストの玉**：籠城の王（−3、自陣から出られない）、鈍重の王（−2、斜めに動けない）。上限が実質13になり編成の幅が広がる。高コスト駒が死んでいる場合に検討
- **二歩御免（1）**二歩の禁止が1回免除される
- **執念の歩（1）**取られた時、自分の持ち駒に戻る
- **蛮勇の王の自陣限定版（1）**前2マスは自陣3段にいる間のみ。入玉が強すぎた場合の代替案

---

## 8. データ構造

### 駒の表現

```
P=歩 L=香 N=桂 S=銀 G=金 K=玉 R=飛 B=角
成り駒は + を前置（+P +L +N +S +R +B）
```

### 盤面の内部表現

```javascript
// board[y][x] : y=0 が後手側の最奥段（段1）、x=0 が左端
// 空マスは null
{
  type: "S",                     // 駒種
  owner: 0,                      // 0=先手, 1=後手
  promoted: false,
  mercenaryId: "dragon_silver",  // 傭兵でなければ null
  abilityLost: false,            // 取られた／成ったで true
  abilityUsed: false             // 一度きりの能力を使用済みか
}
```

**重要**：`mercenaryId` が残っていても `abilityLost: true` なら通常駒として扱う。
表示上「元傭兵」と分かるようにしておくと対局者にとって分かりやすい。

### 傭兵定義（src/data/mercenaries.json）

```json
{
  "dragon_silver": {
    "id": "dragon_silver",
    "name": "龍騎銀",
    "baseType": "S",
    "cost": 9,
    "description": "成ると龍になる。成った時点で傭兵能力は消滅し、以後は龍として振る舞う。",
    "effects": [
      { "type": "promoteTo", "value": "R" }
    ]
  },
  "shield_pawn": {
    "id": "shield_pawn",
    "name": "盾歩",
    "baseType": "P",
    "cost": 5,
    "description": "取られる時、1回だけ耐えてその場に残る。成ると持ち駒に盾歩が1枚補充される。",
    "effects": [
      { "type": "surviveCapture", "times": 1 }
    ],
    "promotionBonus": { "type": "refillMercenary", "mercenaryId": "shield_pawn" }
  }
}
```

**書き方の注意**（間違えやすい点）:

| 項目 | 正しい書き方 |
|---|---|
| `promoteTo` の `value` | **`"R"`**（`"+R"` ではない）。成り記号は付けない |
| 成り特典 | `promotionBonus` に書く。型は **`"refillMercenary"`** |
| 「成ると能力を失う」 | **書かない。** 全傭兵共通のルールとしてコードが処理する |

任意で足せるもの:

| 項目 | 意味 |
|---|---|
| `shortName` | 盤上の名前帯に出す略称（4文字以上の名前に付ける） |
| `maxCount` | 1局で選べる枚数の上限（既定は駒種の枚数とコスト上限まで） |
| `image` | 編成画面のカードに出す専用イラスト（`assets/sprites/` からの相対パス） |

### 効果タイプ（effects.type）

| type | 意味 |
|---|---|
| `extraMove` | 追加の移動パターンを与える |
| `removeMove` | 移動パターンを禁止する |
| `promoteTo` | 成った時の駒種を指定する |
| `promoteAnywhere` | 敵陣以外でも成れる |
| `cannotPromote` | 成れない |
| `surviveCapture` | 取られるのを N 回耐える |
| `vanishOnCapture` | 取られても相手の持ち駒にならない |
| `firstMoveOnly` | 一局の最初の1手のみ有効 |
| `oncePerGame` | 一局に1回のみ |
| `decoyKing` | 玉の身代わりになる |
| `returnToOrigin` | 移動前の位置に戻る |

新しい傭兵を追加する時は、**JSON にエントリを足すだけ**で済む状態を保つこと。
既存の効果タイプで表現できない能力を思いついた場合のみ、コードに効果タイプを追加する。

### 棋譜

```json
{
  "moves": [
    { "from": [4,6], "to": [4,5], "piece": "P", "promote": false },
    { "drop": "P", "to": [3,4] },
    { "from": [1,7], "to": [1,5], "piece": "S", "promote": true, "bonus": "addToHand" }
  ]
}
```

### セーブデータ

```json
{
  "version": 1,
  "savedAt": "2026-09-20T12:34:56.789Z",
  "mode": "vsAI",
  "roster": {
    "sente": ["dragon_silver", "first_charge_pawn"],
    "gote": ["shield_pawn", "diagonal_pawn", "reverse_lance"]
  },
  "board": [ "8行 × 9列" ],
  "hands": {
    "sente": [ { "type": "P", "count": 2 } ],
    "gote": []
  },
  "turn": 0,
  "moveCount": 37,
  "moves": [ "棋譜" ],
  "checksum": "a3f9c2..."
}
```

- 保存先：`localStorage` のキー `shogi_save_slot1`
- エクスポート：`.json` をダウンロード
- インポート：ファイル選択 → `version` 検証 → 確認 → 反映
- `version` を必ず持ち、将来のスキーマ変更に備える

---

## 9. 対局モード

| モード | 内容 |
|---|---|
| CPU戦 | 人間 対 AI。**これがメインのモード** |
| ローカル対戦 | 同じ画面を2人で交互に使う。ネットワークは不要 |

### ローカル対戦を残す理由

リリース時の目玉機能ではないが、**開発とバランス調整に必須**なので必ず実装して残す。

- 特定の局面を手で再現し、傭兵能力の発動を確認できる
- AI の挙動が怪しい時、ルール側のバグか AI 側のバグかを切り分けられる
- コスト調整の際、特定の編成同士を自分で指して感触を確かめられる

実装コストはほぼゼロ（AI を呼ぶ代わりに人間の入力を待つ分岐が1つ増えるだけ）。
オンライン対戦は実装しない（GitHub Pages にサーバー機能がないため）。

---

## 10. AI の設計

### 実行環境

- **Web Worker で実行する。** メインスレッドで動かすと思考中に画面が固まる
- 1手あたりの思考時間は 1〜2秒で打ち切り
- 深さ固定ではなく**反復深化**で、時間制限に達したら直前の深さの結果を返す

### アルゴリズム

- ミニマックス ＋ アルファベータ枝刈り
- 目標は 3〜5手読み。9×9盤なので合法手の数は通常将棋と同程度
- 到達できる強さは初心者〜中級者。プロ級は目指さない

### 評価関数

```
評価値 = 駒の価値 + 位置評価 + 玉の安全度 + 傭兵能力の残存価値
```

**傭兵能力の残存価値が本作固有の難所。** 通常の将棋 AI の評価関数をそのまま流用できない。

- 傭兵は能力を保持している間、コスト相当の加点を持つ
- 取られた／成った時点でその加点が消える
- したがって AI は「成ると能力を失う」ことを正しく計算に入れられる
- 一度きりの能力（盾歩・影武者・帰還桂）は未使用時のみ加点

この設計により、「成って龍になる価値」と「傭兵能力を保つ価値」を同じ尺度で比較できる。

### AI の編成

対局前の編成も AI が決める必要がある。
**初期実装では、あらかじめ用意した数パターンからランダムに選ぶだけでよい。**

```json
[
  { "name": "一点賭け", "roster": ["dragon_silver", "first_charge_pawn"] },
  { "name": "攻撃型",   "roster": ["fang_bishop", "return_knight"] },
  { "name": "バランス", "roster": ["shield_pawn", "diagonal_pawn", "reverse_lance"] },
  { "name": "守備型",   "roster": ["decoy_gold", "brave_king"] }
]
```

### 段階的な実装

| 段階 | 内容 | 目的 |
|---|---|---|
| 1 | 合法手からランダムに選ぶ | **ルール実装の検証**。ここは飛ばさない |
| 2 | 1手先の評価のみで最善手を選ぶ | 弱いが形になる |
| 3 | ミニマックス3手読み | 遊べる強さ |
| 4 | アルファベータ＋反復深化で5手読み | 目標 |

段階1で数百局を自動対戦させ、反則手や無限ループが起きないことを確認する。

---

## 11. ディレクトリ構成

```
/
├── index.html
├── style.css
├── src/
│   ├── main.js
│   ├── core/
│   │   ├── board.js          # 盤面状態、駒の配置
│   │   ├── moves.js          # 合法手生成
│   │   ├── rules.js          # 二歩・打ち歩詰め・千日手・詰み判定
│   │   ├── abilities.js      # 傭兵能力の適用と消滅
│   │   └── game.js           # 対局の進行管理
│   ├── ui/
│   │   ├── boardView.js      # DOM 盤面の描画
│   │   ├── draftView.js      # 編成フェーズ画面（傭兵カード）
│   │   ├── handView.js       # 持ち駒
│   │   ├── logView.js        # 棋譜表示
│   │   ├── labels.js         # 筋・段の呼び名（盤の目盛りと棋譜で共用）
│   │   └── sprite.js         # イラストの切り出し（盤と編成画面で共用）
│   ├── ai/
│   │   ├── worker.js         # Web Worker エントリ
│   │   ├── search.js         # 探索
│   │   ├── evaluate.js       # 評価関数
│   │   └── roster.js         # AI の編成選択
│   └── data/
│       ├── mercenaries.json
│       ├── pieces.json       # 通常駒の名前と成り先
│       └── aiRosters.json
├── assets/
│   ├── sprites/              # 駒のイラスト（WebP）＋差し替え手順
│   ├── fonts/                # 駒字フォントのサブセット＋ライセンス
│   └── CREDITS.md            # 素材の出典・ライセンス
├── tools/
│   ├── dev-server.py         # ローカル確認用（localhost 限定）
│   └── prepare-sprite.py     # 画像の背景透過・WebP化
├── README.md
└── spec.md
```

**未実装のもの**（この構成図にも載せていない）:

- `save.js` … セーブ／ロードは**作らない方針**に変更した。ブラウザを開いたら1局遊ぶ、という形にしている
- `audio.js` と `assets/voice/` … 効果音は未着手（素材の用意が先）

---

## 12. 第1段階の完成条件（DoD）

- [ ] 9×9盤が表示され、初期配置が正しい
- [ ] 通常の将棋の全駒が正しく動く
- [ ] 成り（任意・強制）が正しく動作する
- [ ] 持ち駒の打ち込みができる
- [ ] 二歩・打ち歩詰め・行き所のない駒が禁止される
- [ ] 詰み判定と勝敗表示が動く
- [ ] 千日手の判定が動く
- [ ] 編成フェーズでコスト上限10の範囲で傭兵を選べる
- [ ] 傭兵の能力が正しく発動する
- [ ] 取られた時・成った時に能力が消滅する
- [ ] 盾歩・影武者・帰還桂の一度きりの能力が正しく動く
- [ ] ローカル対戦で一局を最後まで指せる
- [ ] ランダム AI（段階1）と対局できる
- [ ] `renderMode` を text / sprite で切り替えられる
- [ ] 能力を失った元傭兵が見た目で判別できる
- [ ] アニメーションをオフにできる
- [ ] ミュートボタンが動き、設定が保存される
- [ ] GitHub Pages 上で実際に動作する

---

## 13. 実装順の推奨

1. 盤面の DOM 描画と初期配置（傭兵なし、通常の将棋のみ）
2. 合法手生成と移動、持ち駒
3. 成り、二歩、打ち歩詰め、行き所のない駒
4. 詰み判定、千日手、勝敗表示
5. **ここでローカル対戦の通常将棋（9×9）が完成する。必ず動作確認する**（表示は漢字モードのまま）
6. 傭兵データ構造と編成フェーズ UI
7. 能力の適用（移動パターン変更系：斜歩・遊撃香・三方桂・退銀・蛮勇の王）
8. 能力の適用（成り系：先駆歩・龍騎銀・竜牙角・車輪飛）
9. 能力の適用（一度きり系：盾歩・影武者・帰還桂・初手駆けの歩）
10. 能力消滅ルールの実装と検証
11. ~~セーブ／ロード／エクスポート／インポート~~（**見送り**。1局完結の形にした）
12. AI 段階1（ランダム）と自動対戦による検証
13. AI 段階2〜4
14. `renderMode: "sprite"` の実装とイラストの組み込み
15. CSS アニメーション（移動・攻撃・被弾・成り・能力発動）
16. 音声再生と設定（ミュート、アニメーションオフ）
17. 対戦してコスト調整

**14〜16 は最後でよい。** 絵と音が1つもなくても 1〜13 は完成させられる。
逆に見た目から作り始めると、ルールが固まる前に作り直しが発生する。

各ステップの終わりに必ず動作確認して git commit する。

---

## 14. ビジュアル仕様

### 基本方針

将棋駒の五角形はそのまま使い、**漢字が入る位置にキャラクターのイラストを表示する**。
盤全体の見やすさと将棋らしさを保ったまま、キャラクター性を出す。

キャラは**上半身のみ**を大きく描き、その**背後に駒の漢字をうっすら重ねる**。
絵だけでは駒種の判別がつきにくいため、漢字を透かしとして残すことで将棋としての可読性を確保する。
漢字は DOM 側で描画するので、成り駒では「龍」「馬」など正しい表記になる。

イラストは五角形とは別のレイヤーに重ねる。ただし駒の輪郭で切り抜くため、
兜の前立てや武器が画面の端まで伸びていても欠けない。

### 重要な設計判断：キャラは常に正立させる

見下ろし視点では先手・後手で駒の向きが上下反対になるが、
**イラストを180度回転させると顔が逆さまになって成立しない。**

したがって：

- **キャラのイラストは常に正立**させる（回転させない）
- **五角形の駒台だけを回転**させて向きを表現する
- 陣営は**色**でも区別する（先手＝青系、後手＝赤系。同じ絵のパレット差し替え）

この方式なら絵は8枚で両陣営に使える。CPU戦のみでも相手の駒は常に画面上にあるため、
この判断は必須（手を抜ける箇所ではない）。

### サイズ

| 要素 | サイズ |
|---|---|
| 1マス | 最大96px（画面幅に応じて可変） |
| 五角形の駒 | マスの 80%（幅）× 92%（高さ） |
| イラスト | 高さ400〜600px程度の**全身画像**。駒の輪郭で切り抜いて上半身を見せる |

当初はドット絵（32×32px・`image-rendering: pixelated`）を想定していたが、
実際にはキャラクターのイラストを使うことにしたため、**拡大補間は `auto`** にしている。
`pixelated` のままだと縮小時にギザギザになる。

絵ごとに顔の大きさがばらつくため、**顔幅と目線の高さを実測して駒ごとに拡大率を決める**。
計算式と実測値は `assets/sprites/README.md` にある。

### 駒種の描き分け

盤上に40個並ぶので、**ぱっと見で区別できること**が最優先。

| 駒 | イメージ |
|---|---|
| 歩 | 槍を持った軽装の足軽 |
| 香 | 長槍を構えた兵 |
| 桂 | 馬に乗った姿 |
| 銀 | 銀色の鎧、刀 |
| 金 | 金色の鎧、太刀 |
| 角 | 大薙刀 |
| 飛 | 大きな旗を背負う |
| 玉 | 陣羽織、采配 |

### 成り駒

**別絵を用意せず、同じ絵に金色のオーラを CSS で重ねる。**
制作量が増えず、「成った」ことも一目で分かる。

### 傭兵の表示

傭兵は元の駒の絵をベースに、**差分だけ**を足す。

- 装飾（マント、旗印、色違いのアクセント）を重ねる
- 五角形の縁は**金色に固定**し、コストの高低は**縁の太さ**で示す（1〜3＝細 / 4〜6＝中 / 7〜9＝太＋発光）
  - 当初は縁の色でコスト帯を示す案だったが、**赤縁が後手の陣営色（赤）と混ざって判別できなかった**ため変更した。
    陣営色（青・赤）と衝突しない金色に統一している
- 能力を失った元傭兵は装飾を外し、縁をグレーにする

**能力を失ったことが視覚的に分かることは、本作では必須。**
ルールの中核なので、表示で伝わらないとゲームが成立しない。

### 画像形式

- **SVG 推奨**。1枚 3〜10KB、拡大しても劣化せず、色の差し替えが容易
- PNG の場合は 32×32px、1枚 10〜20KB
- 総容量は 16〜30枚で 300KB 程度

### アニメーション（CSS transform のみ。画像は増やさない）

| 動作 | 表現 | 目安 |
|---|---|---|
| 移動 | 移動先へスライド | 0.2秒 |
| 攻撃 | 相手方向へ踏み込んでから戻る | 0.25秒 |
| 被弾 | 白く点滅し、透明度を下げながら消える | 0.3秒 |
| 成り | 一瞬拡大して光る | 0.4秒 |
| 能力発動 | 駒の周囲が脈動する | 0.4秒 |

- 全て `transform` と `opacity` のみで実装（レイアウトを再計算させない）
- **アニメーションをスキップ／オフにできる設定を必ず用意する。** AI の自動対戦テスト時に必要
- `prefers-reduced-motion` にも対応する

### 開発初期の扱い

**絵が1枚もなくても開発を進められる状態を維持する。**

```javascript
renderMode: "text" | "sprite"
```

`text` モードでは漢字を表示する。ルール実装・AI 実装はこのモードで完成させ、
絵が揃ってから `sprite` に切り替える。

---

## 15. ボイス仕様

BGM は実装しない。**効果音（ボイス）のみ**。

### 鳴らすイベント

| イベント | 備考 |
|---|---|
| 駒を取る（攻撃） | |
| 成る | |
| 取られる | |
| 傭兵の能力発動 | **音がないと何が起きたか分からないため重要**（盾歩が耐えた、影武者が身代わりになった等） |
| 王手 | |
| 対局終了 | |

### 音声ファイルの仕様

| 項目 | 決定 |
|---|---|
| 形式 | **mp3**（全ブラウザ対応。ogg より互換性が確実） |
| チャンネル | モノラル |
| ビットレート | 48〜64kbps |
| サンプリングレート | 22050Hz |
| 長さ | 0.5〜1.5秒 |
| 1本あたり | 10〜30KB |

段階的に増やす：

1. 共通3本（取る・成る・取られる）＝ 約60KB
2. 駒種8種 × 3イベント ＝ 24本、約500KB
3. 傭兵ごとの専用ボイス ＝ さらに +800KB

全部盛りでも 1.5MB 程度。容量は問題にならない。

### 実装上の注意

- **iOS Safari は自動再生を禁止している。** 最初のユーザー操作をトリガーに音声をプリロード・アンロックする処理を必ず入れる
- Web Audio API は不要。`Audio` 要素で足りる
- ただし連続再生で音が切れるため、同じ音を複数インスタンス持つか、再生前に `currentTime = 0` でリセットする
- **ミュートボタンは必須。** 設定は localStorage に保存する
- AI の自動対戦テスト時は音声を完全に無効化できるようにする

### 素材の入手とライセンス

**素材のダウンロードは手作業で行う。** Claude にも Claude Code にも代行させない。

- 素材サイトは「人間が規約に同意してダウンロードする」前提で運用されている
- 規約への同意は本人が行う必要がある
- 多くのサイトが自動取得を規約で禁止している

候補：

- 効果音ラボ、魔王魂（フリー効果音）
- VOICEVOX、COEIROINK（無料の合成音声。**キャラごとに規約が異なるため個別に確認**）
- 自分で録音（スマホ＋Audacity で十分）

### ライセンス管理

```
assets/
├── voice/
│   ├── raw/          # ダウンロードした元ファイル（git 管理外でもよい）
│   └── *.mp3         # 変換済み
└── CREDITS.md        # 全素材の出典・ライセンス・作者を記載
```

**`CREDITS.md` は必須。** 公開リポジトリなので、CC-BY 等の表示義務がある素材は必ず記載する。
NC（非商用）条件の素材は、広告なしの個人公開なら問題ないが、避けた方が無難。

ダウンロード後の一括変換・リネームは Claude Code に任せてよい（ffmpeg を使う）。

---

## 16. バランス調整のチェックポイント

実装後、実際に対戦して以下を確認する。

- 蛮勇の王で入玉が簡単になりすぎていないか
- **9×9に戻したことで、8段盤を前提に決めたコストがずれていないか。**
  特に「初手駆けの歩1」「蛮勇の王3」は、中立が1段増えたぶん価値が下がっているはず
- 龍騎銀9・竜牙角8・車輪飛8 が強すぎ／弱すぎないか
- 遊撃香4 が斜め4方向を得て強くなりすぎていないか
- コスト1〜3の安い傭兵が「端数の受け皿」として機能しているか
- 上限10が適切か（12や15の方が編成が面白くなる可能性がある）
- 傭兵なし（コスト0）の構成が不利になりすぎていないか
