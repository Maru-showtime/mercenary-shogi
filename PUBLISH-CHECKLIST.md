# 公開前チェックリスト

GitHub に上げる前に、**毎回**これを上から順に実行する。
「今回は小さい変更だから」で飛ばさない。過去に実際に事故が起きている（下記）。

---

## 1. 機械検査（必須・最優先）

```
python tests/security-scan.py
```

**「検出 0 件」でなければ上げない。**

### 初回だけ必要な設定

ユーザー名やフォルダ名は自動で検査対象になりますが、**漢字の氏名などパスから導けない語**は
ホームディレクトリの次のファイルに1行1語で書いてください。

```
%USERPROFILE%\.publish-private-words.txt      （Windows）
~/.publish-private-words.txt                  （Mac / Linux）
```

**リポジトリの外に置くのが要点です。** 中に置くとドラッグ&ドロップで一緒に公開されます
（`.gitignore` は git を使わない運用では効きません）。

未設定のまま実行すると警告が出ます。

### 検査するもの

| 項目 | 例 |
|---|---|
| 氏名・所属 | 実名、会社名。実行環境から自動で導出する |
| メールアドレス | 会社・個人のアドレス |
| ローカル絶対パス | Windows や Mac のユーザーフォルダのパス（ユーザー名が露出する） |
| プライベートIP | `192.168.x.x`、`10.x.x.x` |
| 鍵・トークン | `api_key = "..."`、AWS/GCPのキー形式 |
| 画像のメタデータ | EXIF に残る作者名・ソフト名 |

## 2. 中身の検査

```
python tests/check-rosters.py       # AI編成が実在する傭兵か・コスト上限内か
python tests/audit-docs.py          # ドキュメントの数値が実データと合っているか
python tests/audit-json-samples.py  # spec.md のJSONサンプルが実データと同じ書き方か
```

## 3. 動作の検査

```
node tests/test-core.mjs
node tests/test-abilities.mjs
node tests/test-ai.mjs
```

## 4. 目視

- [ ] 画面に出る文章（`index.html` の「傭兵将棋とは」）が今の仕様と合っているか
- [ ] `assets/CREDITS.md` の素材の出典が埋まっているか

## 5. アップロード

フォルダの**中身**を全選択してドラッグする。フォルダごと投げない
（`mercenary-shogi/index.html` という階層になり、GitHub Pages が真っ白になる）。

## 6. 公開後の確認

シークレットウィンドウで https://maru-showtime.github.io/mercenary-shogi/ を開いて、
意図した内容になっているか確認する。

---

## なぜこのファイルがあるか

**2026/09/22、素材クレジットに実名を書いたまま約26時間・6コミット公開した。**
指摘されるまで気付かなかった。

この事故から学んだこと:

- **公開の可否は最後に点検する工程ではない。** 公開先に入る文字を1字書いた時点で決まる
- **ファイルを直しても履歴には残る。** GitHub は古いコミットのURLで中身が読める。
  本当に消すにはリポジトリの再作成が要る
- **`.gitignore` はこのプロジェクトでは無力。** git を使わずWebのドラッグ&ドロップで
  上げているため、除外指定は一切効かない。「gitignore したから安全」は成り立たない
- **秘密を守る仕組みに、その秘密自体を書かない。** 検査スクリプトに氏名を直書きすると、
  スクリプトが漏洩源になる。`tests/security-scan.py` は実行環境から導出している

## 帰属表示の書き方

素材の出典を書くとき、**実名を使わない。**

```
NG   Google Gemini による生成画像（山田が生成、2026年）
OK   Google Gemini による生成画像（本リポジトリ作者が生成、2026年）
```

記録としての意味は変わらない。実名が必要な場面は、公開リポジトリにはほぼ無い。
